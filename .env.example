# URL of the SocialFlow backend (Express API on Railway, or localhost in dev)
VITE_API_URL=https://socialflowbk-production.up.railway.app/

# Public URL of this frontend itself (Vercel domain, or localhost in dev).
# Used to build things like absolute links; safe to expose since it's public.
VITE_APP_URL=http://localhost:5173
